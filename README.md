# Install
```shell
sudo gem install bundle
sudo gem install jekyll
sudo gem update
sudo bundle update
sudo bundle install
```

# Startup
```shell
nohup bundle exec jekyll serve --host=0.0.0.0 --port=80 &
```